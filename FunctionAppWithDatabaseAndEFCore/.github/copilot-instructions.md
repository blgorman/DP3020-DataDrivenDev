# Copilot Instructions

## Project Guidelines
- User prefers using Package Manager Console (PMC) for EF Core migrations, not the dotnet-ef CLI tool. They need Microsoft.EntityFrameworkCore.Tools (not dotnet-ef) for PMC commands like Add-Migration.